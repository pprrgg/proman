{
  "name": "vatiaco-docs-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "predeploy": "scripts/set-docs-url.sh prod && astro build && rm -rf dist/docs/A00_Blog_Interactivo/z",
    "deploy": "gh-pages -d dist --dotfiles",
    "postdeploy": "scripts/set-docs-url.sh local"
  },
  "devDependencies": {
    "astro": "^4.8.0",
    "gh-pages": "^6.3.0"
  }
}
